
from app.common.base_transport import BaseTransport
from app.modules.{{name}}.storage.{{name}}_storage import {{model}}Store
from app.modules.{{name}}.model import {{model}}Create,{{model}}Update,{{model}}

class {{model}}Transport(BaseTransport):
    def __init__(self) -> None:
        self.store = {{model}}Store({{model}})
        super().__init__(self.store)

    def get_router(self):
        return self.router


{{name}}_transport = {{model}}Transport()
{{name}}_transport.configure_routes({{model}}Create,{{model}}Update)